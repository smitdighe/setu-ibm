# AGENTS.md — Ask mode

This file provides guidance to agents when working with code in this repository.

<!-- Fill in non-obvious documentation context.
     Only include things that would mislead a developer reading the file structure.
     Delete this comment once populated. -->

## Counterintuitive Structure

<!-- Example:
     - `src/` contains the VSCode extension, NOT the web app source
     - `lib/` is auto-generated — editing files there will be overwritten
-->

## Misleading Names

<!-- Example:
     - `packages/client` is the server-side rendering layer, not a browser client
     - `scripts/build.sh` runs tests as a side-effect before building
-->

## Documentation Location

<!-- Where canonical reference lives if it's not obvious.
     Example:
     - Provider examples in `src/providers/` are authoritative; the `/docs` folder is outdated
-->
